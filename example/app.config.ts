import { ExpoConfig } from 'expo/config';

const config: ExpoConfig = {
  name: 'expo-stable-id-example',
  slug: 'expo-stable-id-example',
  version: '1.0.0',
  plugins: ['@nauverse/expo-stable-id'],
};

export default config;
